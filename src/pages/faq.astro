---
import Base from '../layouts/Base.astro';
import Faq from '../components/Faq.astro';
import { FAQ, faqSchema } from '../lib/faq';
import { SITE } from '../config/brand';
---

<Base
  title="Frequently asked questions"
  path="/faq"
  description={`Answers about ${SITE.name}, natural fibers, women and men pieces, sizing, shipping, returns, new daily drops, and one of a kind availability.`}
  jsonLd={[faqSchema()]}
>
  <section class="mx-auto max-w-7xl px-4 pb-4 pt-8 sm:px-6 md:pt-12">
    <div class="relative overflow-hidden rounded-panel border border-brown/15 bg-blue/60 px-6 py-14 text-center sm:px-10 md:py-20">
      <div class="absolute -left-24 -top-32 size-80 rounded-full border border-brown/10"></div>
      <div class="absolute -bottom-36 -right-24 size-96 rounded-full border border-brown/10"></div>
      <div class="relative mx-auto max-w-3xl">
        <p class="eyebrow">The full FAQ</p>
        <h1 class="mt-5 font-display text-5xl leading-tight tracking-[-0.03em] text-ink md:text-7xl">
          Everything to know before your find becomes yours.
        </h1>
        <p class="mx-auto mt-6 max-w-2xl text-base leading-8 text-brown">
          Straight answers about Tati Curated, the pieces, shipping, sizing, discounts, and what happens when an item sells.
        </p>
      </div>
    </div>
  </section>

  <Faq
    items={FAQ}
    eyebrow="All the details"
    title="Questions, answered clearly."
    intro="Still wondering about something? Visit Tati on Depop for another way to connect and shop."
    showAllLink={false}
  />

  <section class="mx-auto max-w-4xl px-4 pb-20 sm:px-6 md:pb-28">
    <div class="rounded-panel bg-ink p-8 text-center text-ivory md:p-12">
      <p class="font-display text-3xl md:text-4xl">Ready to meet your next favorite piece.</p>
      <div class="mt-7 flex flex-col justify-center gap-3 sm:flex-row">
        <a href="/shop" class="inline-flex min-h-12 items-center justify-center rounded-lg bg-ivory px-7 text-sm font-bold text-ink transition hover:bg-paper">
          Shop the full edit
        </a>
        {SITE.depop && (
          <a href={SITE.depop} rel="noopener noreferrer" class="inline-flex min-h-12 items-center justify-center rounded-lg border border-ivory/30 px-7 text-sm font-bold text-ivory transition hover:border-ivory">
            Visit Tati on Depop
          </a>
        )}
      </div>
    </div>
  </section>
</Base>
